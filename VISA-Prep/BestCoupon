bill_amount = float(input().strip())
discount_10_percent = 0.10 * bill_amount
flat_discount_100 = 100
max_discount = max(discount_10_percent, flat_discount_100)
final_amount = bill_amount - max_discount
print(int(final_amount))
